# Locust and its core dependencies.
# The exact pins below are validated on Python 3.11. On Python 3.14,
# gevent may fail to build — install the latest locust from PyPI without
# a pinned gevent, or use a managed install (e.g. pipx install locust).
locust==2.43.3
gevent>=24.2.0
geventhttpclient>=2.3.0
msgpack>=1.0.0
psutil>=5.9.0
requests>=2.32.0
roundrobin>=0.0.4
typing_extensions>=4.12.0
urllib3>=2.2.0
Werkzeug>=3.0.0
zope.event>=5.0
zope.interface>=7.0

# Real-browser load testing (loadtest/locustfile_browser.py).
# playwright is a hard dep for locustfile_browser.py; the HTTP-only
# locustfile.py does not need it.
playwright>=1.45.0
